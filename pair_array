#Find All Pairs in Array That Sum to a Given Number (say 25)
arr = [10,15,7,8,12,13,5,20]

target = 25

for i in range(len(arr)):
    for j in range(i+1,len(arr)):
        if(arr[i]+arr[j]) == target:
            print({arr[i],arr[j]})
